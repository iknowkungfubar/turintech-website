// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://www.turintechsolutions.com',
  build: {
    format: 'directory',
  },
});
